<%= @thought.body %>
<%= link_to "Edit Thought", edit_thought_path(@thought) if current_user.id == @thought.user_id %>
<%= link_to "Delete thought", thought_path(@thought), method: :delete unless current_user == @thought.user %>
